import React from 'react'

function Navbar() {
  return (
    <header>
        <div>Doctor App</div>
        <div>
            <button>Home</button>
            <button>Add doctor</button>
        </div>
    </header>
  )
}

export default Navbar